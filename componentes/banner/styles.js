import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    containerBanner: {
      alignItems: 'center',
      marginTop: 15,
    },
    imageBanner: {
      width: 400,
      height: 200,
      marginTop: 15,
      borderRadius: 10,
    },
    TextBanner: {
      fontSize: 30,
      color: 'white',
      fontWeight: 'bold',
      textAlign: 'center',
    },  
  });
  

export default styles;